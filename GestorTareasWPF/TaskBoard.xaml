<UserControl x:Class="GestorTareasWPF.TaskBoard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#E0FBFC" Offset="0.0"/>
                <GradientStop Color="#C2E9FB" Offset="1.0"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="White" Padding="20,15" Margin="0,0,0,10">
            <Border.Effect>
                <DropShadowEffect Color="#DDDDDD" BlurRadius="10" ShadowDepth="3" Direction="270"/>
            </Border.Effect>
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <Border Width="8" Height="30" Background="{StaticResource SecondaryBrush}" CornerRadius="4" Margin="0,0,10,0"/>
                    <TextBlock Text="TaskFlow" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="+ Nueva Tarea" Click="BtnNuevaTarea_Click" Background="{StaticResource SecondaryBrush}"/>
                    <Button Content="⚙ Config" Click="BtnConfigurar_Click" Background="{StaticResource AccentBrush}" Foreground="#333"/>
                    <Button Content="📊 Informe" Click="BtnInforme_Click" Background="{StaticResource PrimaryBrush}"/>
                </StackPanel>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" PanningMode="HorizontalOnly">
            <Grid x:Name="gridColumnas" Margin="10,0,10,20">
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>