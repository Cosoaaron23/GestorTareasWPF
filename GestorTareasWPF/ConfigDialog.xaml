<Window x:Class="GestorTareasWPF.ConfigDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configurar Columnas" Height="350" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Border Padding="15">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Gestionar Columnas" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

            <ListBox x:Name="lstColumnas" Grid.Row="1" Margin="0,0,0,10" BorderBrush="#CCC" BorderThickness="1"/>

            <Grid Grid.Row="2" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="txtNuevaColumna" Padding="5" ToolTip="Nombre de nueva columna"/>
                <Button Content="Agregar" Click="BtnAgregar_Click" Grid.Column="1" Margin="5,0" Padding="10,5"/>
                <Button Content="Eliminar Seleccionada" Click="BtnEliminar_Click" Grid.Column="2" Padding="10,5" Background="#FFEBEE" Foreground="Red"/>
            </Grid>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancelar" IsCancel="True" Padding="15,5" Margin="0,0,10,0"/>
                <Button Content="Guardar Configuración" Click="BtnGuardar_Click" Padding="15,5" Background="#2196F3" Foreground="White" FontWeight="Bold"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>